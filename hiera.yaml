---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:
  - name: "Major Version"
    backend: yaml
    path: "%{facts.os.name}-%{facts.os.release.major}.yaml"
  - name: "Operating System Family"
    backend: yaml
    path: "%{facts.os.family}.yaml"
  - name: "common"
    backend: yaml
    path: "common.yaml"
